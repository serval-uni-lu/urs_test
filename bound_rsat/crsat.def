BootStrap: docker
From: ubuntu:22.04

%files
    ./ /crsat

%post
    echo "setup start"
    apt-get -y update
    apt-get -y install make gcc libz3-dev libbsd-dev pkg-config

    cd crsat
    make clean
    make -j8

    ls
    echo "setup done"

%runscript
    /crsat/crsat "$@"
